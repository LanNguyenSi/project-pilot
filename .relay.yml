name: project-pilot
health: /api/health
health_port: 3001
compose_file: docker-compose.prod.yml
post_update:
  - docker compose -f docker-compose.prod.yml exec -e HOME=/tmp backend npx prisma db push --schema=prisma/schema.prisma
rollback: true
